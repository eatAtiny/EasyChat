spring:
  application:
    name: group-service # 服务名称
  cloud:
    nacos:
      server-addr: 172.23.80.100 # nacos地址
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: shared-log.yaml # 共享日志配置